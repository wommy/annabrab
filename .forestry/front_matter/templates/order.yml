---
label: work
hide_body: true
fields:
- name: work
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
  - name: title
    type: text
    config:
      required: false
    label: title
  - name: description
    type: text
    config:
      required: false
    label: description
  - name: landscape
    type: boolean
    label: landscape
  config:
    min: 
    max: 
    labelField: title
  label: work
pages:
- src/11ty/_data/work.json
