---
label: order
hide_body: true
fields:
- name: order
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: documents
      section: work
      file: 
      path: ''
  label: order
- name: gallery
  type: image_gallery
  config: {}
  label: gallery
- name: fg
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
  - name: title
    type: text
    config:
      required: false
    label: title
  - name: description
    type: text
    config:
      required: false
    label: description
  config:
    min: 
    max: 
    labelField: title
  label: fg
pages:
- src/11ty/_data/work.json
