---
label: work
hide_body: true
fields:
- name: order
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
  - name: title
    type: text
    config:
      required: false
    label: title
  - name: description
    type: text
    config:
      required: false
    label: description
  - name: landscape
    type: boolean
    label: landscape
  - name: year
    type: datetime
    label: year
    description: ''
    config:
      required: false
      date_format: M/YYYY
      time_format: 
      display_utc: false
  config:
    min: 
    max: 
    labelField: title
  label: order
pages:
- src/11ty/_data/work.json
