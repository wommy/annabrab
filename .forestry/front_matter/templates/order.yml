---
label: order
hide_body: false
fields:
- name: order
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: documents
      section: work
      file: 
      path: ''
  label: order
- name: work
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
  - name: title
    type: text
    config:
      required: false
    label: title
  - name: description
    type: text
    config:
      required: false
    label: description
  config:
    min: 
    max: 
    labelField: title
  label: work
pages:
- src/11ty/_data/work.json
