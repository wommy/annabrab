---
label: order
hide_body: true
fields:
- name: order
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: documents
      section: work
      file: 
      path: ''
  label: order
- name: gallery
  type: image_gallery
  config: {}
  label: gallery
- name: blocks
  type: blocks
  label: blocks
  template_types:
  - work
  - order
  config:
    min: 
    max: 
pages:
- src/11ty/_data/work.json
